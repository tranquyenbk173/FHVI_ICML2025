# pytorch_lightning==2.0.2
trainer:
  accelerator: gpu
  devices: 1
  logger:
    class_path: pytorch_lightning.loggers.CSVLogger
    init_args:
      save_dir: output/
      name: cifar
  max_epochs: 200
  val_check_interval: 50
  num_sanity_val_steps: 0
model:
  model_name: vit-b16-224-dino
  training_mode: lora
  optimizer: fhbi
  lr: 0.001
  momentum: 0.9
  weight_decay: 0.5
  warmup_steps: 500
  label_smoothing: 0.1
  rho: 1e-5
  scheduler: cosine
  lora_r: 8
  lora_alpha: 8
  num_particles: 4
  max_num_models: 20
  sigma: 1.2
  base_optimizer_name: adamw2
data:
  transform_train_id: 0
  dataset: cifar100
  root: /vast/hvp2011/t/vtab-1k/cifar100
  size: 224
  batch_size: 64
  workers: 4
model_checkpoint:
  dirpath: saved_checkpoints/cifar
  filename: best-step-{step}-{val_acc:.4f}
  monitor: val_acc
  mode: max
  save_last: true